spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres
    username: ${POSTGRES_DEPLOY_DB_USERNAME}
    password: ${POSTGRES_DEPLOY_DB_PW}
    #username: ${POSTGRES_LOCAL_DB_USERNAME}
    #password: ${POSTGRES_LOCAL_DB_PW}
  jpa:
    database-platform: postgres
    show-sql: true
    hibernate:
      ddl-auto: create   # create / update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    generate-ddl: false